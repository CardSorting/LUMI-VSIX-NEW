import React, { memo } from "react"

export interface ModModeSwitcherProps {
	className?: string
}

/**
 * Mode switcher placeholder (only standard Coding mode is used).
 */
export const ModModeSwitcher: React.FC<ModModeSwitcherProps> = memo(() => {
	return null
})

ModModeSwitcher.displayName = "ModModeSwitcher"
